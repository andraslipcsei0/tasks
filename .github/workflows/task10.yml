name: "Parameterized workflow with secrets"
on:
  workflow_dispatch
jobs:
  job:
    env:
      ENVSECRET1: "maci"
      ENVSECRET2: ${{ secrets.ENV_SECRET2 }}
    runs-on: windows-latest
    steps:
      - run: echo ${{ env.ENVSECRET1 }}
        shell: cmd
      - run: echo ${{ env.ENVSECRET2 }}
        shell: cmd
      - run: echo ${{ secrets.REP_SECRET1 }}
        shell: cmd
      - run: echo ${{ secrets.REP_SECRET2 }}
        shell: cmd
